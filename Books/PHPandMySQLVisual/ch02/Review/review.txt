1. method attribute determines the HTTP method server receives
action attribute is set to PHP file (i.e. handle_form.php)

3. Double quotes will evaluate variables contained inside them.
Single quotes interpret all characters literally - except for
\' and \\

10. Superglobals are built-in variables that are always available
in all scopes.

11. count() - provides count of elements in an array